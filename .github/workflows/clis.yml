on: [push]
name: CLIs
jobs:


  meta-check:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash -euo pipefail {0}
    steps:
    - uses: actions/checkout@v4
    - run: ./hack/clis.sh | tee .github/workflows/clis.yml
    - run: ./hack/self.sh | tee .github/workflows/self.yml
    - run: git --no-pager diff --exit-code
    - name: Run shellcheck
      uses: ludeeus/action-shellcheck@2.0.0
      with:
        check_together: 'yes'
        severity: error

  bin:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash -euo pipefail {0}
    steps:
    - uses: actions-rs/toolchain@v1
      with:
        profile: minimal
        toolchain: stable

    - uses: actions/checkout@v4

    - name: Cache `cargo fetch`
      uses: actions/cache@v4
      with:
        path: |
          ~/.cargo/registry/index/
          ~/.cargo/registry/cache/
          ~/.cargo/git/db/
        key: ${{ github.job }}-${{ runner.os }}-cargo-deps-${{ hashFiles('**/Cargo.lock') }}
        restore-keys: ${{ github.job }}-${{ runner.os }}-cargo-deps-

    - name: Cache `cargo install`
      uses: actions/cache@v4
      with:
        path: ~/instmp
        key: ${{ runner.os }}-cargo-install-${{ hashFiles('**/Cargo.lock') }}
        restore-keys: |
          ${{ runner.os }}-cargo-install-

    - name: Compile HEAD cargo-green
      run: |
        CARGO_TARGET_DIR=~/instmp cargo install --locked --force --path=./cargo-green
    - run: ls -lha ~/instmp/release/
    - run: ls -lha /home/runner/.cargo/bin/

    - uses: actions/upload-artifact@v4
      with:
        name: bin-artifacts
        path: /home/runner/.cargo/bin/cargo-green

  buildxargs_master:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash -euo pipefail {0}
    needs: bin
    steps:
    - uses: actions-rs/toolchain@v1
      with:
        profile: minimal
        toolchain: stable


    - name: Retrieve saved bin
      uses: actions/download-artifact@v4
      with:
        name: bin-artifacts

    - name: Install saved bin
      shell: bash -eu {0}
      run: | # TODO: whence https://github.com/actions/download-artifact/issues/236
        chmod +x ./cargo-green
        { ! ./cargo-green --version ; } | grep cargo-green
        mv ./cargo-green ~/.cargo/bin/
    - uses: actions/checkout@v4
    - run: docker info
    - run: docker buildx version
    - run: docker buildx build --help
    - run: podman version || true
    - run: rustc -Vv

    - name: Envs
      run: /home/runner/.cargo/bin/cargo-green green supergreen env
    - name: Envs again
      run: /home/runner/.cargo/bin/cargo-green green supergreen env

    - run: sudo du -sh /var/lib/docker
    - run: docker system df
    - run: docker system df --verbose
    - run: |
        docker buildx du | head || true
        docker buildx du | tail || true
    - run: docker buildx du --verbose
    - name: cargo install net=ON cache=OFF remote=OFF jobs=1
      run: |
        CARGOGREEN_LOG=trace \
        RUSTCBUILDX_LOG_PATH="$PWD"/logs.txt \
          CARGO_TARGET_DIR=~/instst cargo green -vv install --jobs=1 --locked --force buildxargs --git https://github.com/fenollp/buildxargs.git |& tee _
    - if: ${{ failure() || success() }}
      name: 🔴 =means=> it's again that cargo issue https://github.com/rust-lang/cargo/pull/14322
      run: |
        ! grep -C20 -F "thread 'main' panicked at src/cargo/util/dependency_queue.rs:" _

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> it's again that docker issue https://github.com/moby/buildkit/issues/5217
      run: |
        ! grep -C20 -F 'ResourceExhausted: grpc: received message larger than max' logs.txt

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> there's some panic!s
      run: |
        ! grep -C20 -F ' panicked at ' logs.txt

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> there's some BUGs
      run: |
        ! grep -C20 -F 'BUG: ' logs.txt

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> here's relevant logs
      run: |
        ! grep -C20 -F ' >>> ' logs.txt

    - if: ${{ failure() || success() }}
      run: tail -n9999999 logs.txt ; echo >logs.txt
    - run: sudo du -sh /var/lib/docker
    - run: docker system df
    - run: docker system df --verbose
    - run: |
        docker buildx du | head || true
        docker buildx du | tail || true
    - run: docker buildx du --verbose

    - name: Target dir disk usage
      if: ${{ failure() || success() }}
      run: du -sh ~/instst

    - name: Ensure running the same command twice without modifications...
      run: |
        CARGOGREEN_LOG=trace \
        RUSTCBUILDX_LOG_PATH="$PWD"/logs.txt \
          CARGO_TARGET_DIR=~/instst cargo green -vv install --jobs=1 --locked --force buildxargs --git https://github.com/fenollp/buildxargs.git |& tee _
    - name: ...doesn't recompile anything
      run: |
        err=0
        grep Finished  _ | grep -E [01]...s || err=1
        grep Dirty     _                    && err=1
        grep Compiling _                    && err=1
        exit $err
    - if: ${{ failure() || success() }}
      name: 🔴 =means=> it's again that cargo issue https://github.com/rust-lang/cargo/pull/14322
      run: |
        ! grep -C20 -F "thread 'main' panicked at src/cargo/util/dependency_queue.rs:" _

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> it's again that docker issue https://github.com/moby/buildkit/issues/5217
      run: |
        ! grep -C20 -F 'ResourceExhausted: grpc: received message larger than max' logs.txt

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> there's some panic!s
      run: |
        ! grep -C20 -F ' panicked at ' logs.txt

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> there's some BUGs
      run: |
        ! grep -C20 -F 'BUG: ' logs.txt

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> here's relevant logs
      run: |
        ! grep -C20 -F ' >>> ' logs.txt

    - if: ${{ failure() || success() }}
      run: tail -n9999999 logs.txt ; echo >logs.txt
    - run: sudo du -sh /var/lib/docker
    - run: docker system df
    - run: docker system df --verbose
    - run: |
        docker buildx du | head || true
        docker buildx du | tail || true
    - run: docker buildx du --verbose

    - name: Target dir disk usage
      if: ${{ failure() || success() }}
      run: du -sh ~/instst

  cargo-llvm-cov_0-5-36:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash -euo pipefail {0}
    needs: bin
    steps:
    - uses: actions-rs/toolchain@v1
      with:
        profile: minimal
        toolchain: stable
    - run: rustup component add llvm-tools-preview

    - name: Retrieve saved bin
      uses: actions/download-artifact@v4
      with:
        name: bin-artifacts

    - name: Install saved bin
      shell: bash -eu {0}
      run: | # TODO: whence https://github.com/actions/download-artifact/issues/236
        chmod +x ./cargo-green
        { ! ./cargo-green --version ; } | grep cargo-green
        mv ./cargo-green ~/.cargo/bin/
    - uses: actions/checkout@v4
    - run: docker info
    - run: docker buildx version
    - run: docker buildx build --help
    - run: podman version || true
    - run: rustc -Vv

    - name: Envs
      run: /home/runner/.cargo/bin/cargo-green green supergreen env
    - name: Envs again
      run: /home/runner/.cargo/bin/cargo-green green supergreen env

    - run: sudo du -sh /var/lib/docker
    - run: docker system df
    - run: docker system df --verbose
    - run: |
        docker buildx du | head || true
        docker buildx du | tail || true
    - run: docker buildx du --verbose
    - name: cargo install net=ON cache=OFF remote=OFF jobs=1
      run: |
        CARGOGREEN_LOG=trace \
        RUSTCBUILDX_LOG_PATH="$PWD"/logs.txt \
          CARGO_TARGET_DIR=~/instst cargo green -vv install --jobs=1 --locked --force cargo-llvm-cov@0.5.36  |& tee _
    - if: ${{ failure() || success() }}
      name: 🔴 =means=> it's again that cargo issue https://github.com/rust-lang/cargo/pull/14322
      run: |
        ! grep -C20 -F "thread 'main' panicked at src/cargo/util/dependency_queue.rs:" _

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> it's again that docker issue https://github.com/moby/buildkit/issues/5217
      run: |
        ! grep -C20 -F 'ResourceExhausted: grpc: received message larger than max' logs.txt

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> there's some panic!s
      run: |
        ! grep -C20 -F ' panicked at ' logs.txt

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> there's some BUGs
      run: |
        ! grep -C20 -F 'BUG: ' logs.txt

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> here's relevant logs
      run: |
        ! grep -C20 -F ' >>> ' logs.txt

    - if: ${{ failure() || success() }}
      run: tail -n9999999 logs.txt ; echo >logs.txt
    - run: sudo du -sh /var/lib/docker
    - run: docker system df
    - run: docker system df --verbose
    - run: |
        docker buildx du | head || true
        docker buildx du | tail || true
    - run: docker buildx du --verbose

    - name: Target dir disk usage
      if: ${{ failure() || success() }}
      run: du -sh ~/instst

    - name: Ensure running the same command twice without modifications...
      run: |
        CARGOGREEN_LOG=trace \
        RUSTCBUILDX_LOG_PATH="$PWD"/logs.txt \
          CARGO_TARGET_DIR=~/instst cargo green -vv install --jobs=1 --locked --force cargo-llvm-cov@0.5.36  |& tee _
    - name: ...doesn't recompile anything
      run: |
        err=0
        grep Finished  _ | grep -E [01]...s || err=1
        grep Dirty     _                    && err=1
        grep Compiling _                    && err=1
        exit $err
    - if: ${{ failure() || success() }}
      name: 🔴 =means=> it's again that cargo issue https://github.com/rust-lang/cargo/pull/14322
      run: |
        ! grep -C20 -F "thread 'main' panicked at src/cargo/util/dependency_queue.rs:" _

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> it's again that docker issue https://github.com/moby/buildkit/issues/5217
      run: |
        ! grep -C20 -F 'ResourceExhausted: grpc: received message larger than max' logs.txt

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> there's some panic!s
      run: |
        ! grep -C20 -F ' panicked at ' logs.txt

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> there's some BUGs
      run: |
        ! grep -C20 -F 'BUG: ' logs.txt

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> here's relevant logs
      run: |
        ! grep -C20 -F ' >>> ' logs.txt

    - if: ${{ failure() || success() }}
      run: tail -n9999999 logs.txt ; echo >logs.txt
    - run: sudo du -sh /var/lib/docker
    - run: docker system df
    - run: docker system df --verbose
    - run: |
        docker buildx du | head || true
        docker buildx du | tail || true
    - run: docker buildx du --verbose

    - name: Target dir disk usage
      if: ${{ failure() || success() }}
      run: du -sh ~/instst

  cross_0-2-5:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash -euo pipefail {0}
    needs: bin
    steps:
    - uses: actions-rs/toolchain@v1
      with:
        profile: minimal
        toolchain: stable


    - name: Retrieve saved bin
      uses: actions/download-artifact@v4
      with:
        name: bin-artifacts

    - name: Install saved bin
      shell: bash -eu {0}
      run: | # TODO: whence https://github.com/actions/download-artifact/issues/236
        chmod +x ./cargo-green
        { ! ./cargo-green --version ; } | grep cargo-green
        mv ./cargo-green ~/.cargo/bin/
    - uses: actions/checkout@v4
    - run: docker info
    - run: docker buildx version
    - run: docker buildx build --help
    - run: podman version || true
    - run: rustc -Vv

    - name: Envs
      run: /home/runner/.cargo/bin/cargo-green green supergreen env
    - name: Envs again
      run: /home/runner/.cargo/bin/cargo-green green supergreen env

    - run: sudo du -sh /var/lib/docker
    - run: docker system df
    - run: docker system df --verbose
    - run: |
        docker buildx du | head || true
        docker buildx du | tail || true
    - run: docker buildx du --verbose
    - name: cargo install net=ON cache=OFF remote=OFF jobs=1
      run: |
        CARGOGREEN_LOG=trace \
        RUSTCBUILDX_LOG_PATH="$PWD"/logs.txt \
          CARGO_TARGET_DIR=~/instst cargo green -vv install --jobs=1 --locked --force cross@0.2.5 --git https://github.com/cross-rs/cross.git --tag=v0.2.5 cross |& tee _
    - if: ${{ failure() || success() }}
      name: 🔴 =means=> it's again that cargo issue https://github.com/rust-lang/cargo/pull/14322
      run: |
        ! grep -C20 -F "thread 'main' panicked at src/cargo/util/dependency_queue.rs:" _

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> it's again that docker issue https://github.com/moby/buildkit/issues/5217
      run: |
        ! grep -C20 -F 'ResourceExhausted: grpc: received message larger than max' logs.txt

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> there's some panic!s
      run: |
        ! grep -C20 -F ' panicked at ' logs.txt

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> there's some BUGs
      run: |
        ! grep -C20 -F 'BUG: ' logs.txt

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> here's relevant logs
      run: |
        ! grep -C20 -F ' >>> ' logs.txt

    - if: ${{ failure() || success() }}
      run: tail -n9999999 logs.txt ; echo >logs.txt
    - run: sudo du -sh /var/lib/docker
    - run: docker system df
    - run: docker system df --verbose
    - run: |
        docker buildx du | head || true
        docker buildx du | tail || true
    - run: docker buildx du --verbose

    - name: Target dir disk usage
      if: ${{ failure() || success() }}
      run: du -sh ~/instst

    - name: Ensure running the same command twice without modifications...
      run: |
        CARGOGREEN_LOG=trace \
        RUSTCBUILDX_LOG_PATH="$PWD"/logs.txt \
          CARGO_TARGET_DIR=~/instst cargo green -vv install --jobs=1 --locked --force cross@0.2.5 --git https://github.com/cross-rs/cross.git --tag=v0.2.5 cross |& tee _
    - name: ...doesn't recompile anything
      run: |
        err=0
        grep Finished  _ | grep -E [01]...s || err=1
        grep Dirty     _                    && err=1
        grep Compiling _                    && err=1
        exit $err
    - if: ${{ failure() || success() }}
      name: 🔴 =means=> it's again that cargo issue https://github.com/rust-lang/cargo/pull/14322
      run: |
        ! grep -C20 -F "thread 'main' panicked at src/cargo/util/dependency_queue.rs:" _

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> it's again that docker issue https://github.com/moby/buildkit/issues/5217
      run: |
        ! grep -C20 -F 'ResourceExhausted: grpc: received message larger than max' logs.txt

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> there's some panic!s
      run: |
        ! grep -C20 -F ' panicked at ' logs.txt

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> there's some BUGs
      run: |
        ! grep -C20 -F 'BUG: ' logs.txt

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> here's relevant logs
      run: |
        ! grep -C20 -F ' >>> ' logs.txt

    - if: ${{ failure() || success() }}
      run: tail -n9999999 logs.txt ; echo >logs.txt
    - run: sudo du -sh /var/lib/docker
    - run: docker system df
    - run: docker system df --verbose
    - run: |
        docker buildx du | head || true
        docker buildx du | tail || true
    - run: docker buildx du --verbose

    - name: Target dir disk usage
      if: ${{ failure() || success() }}
      run: du -sh ~/instst

  hickory-dns_0-25-0-alpha-1:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash -euo pipefail {0}
    needs: bin
    steps:
    - uses: actions-rs/toolchain@v1
      with:
        profile: minimal
        toolchain: stable


    - name: Retrieve saved bin
      uses: actions/download-artifact@v4
      with:
        name: bin-artifacts

    - name: Install saved bin
      shell: bash -eu {0}
      run: | # TODO: whence https://github.com/actions/download-artifact/issues/236
        chmod +x ./cargo-green
        { ! ./cargo-green --version ; } | grep cargo-green
        mv ./cargo-green ~/.cargo/bin/
    - uses: actions/checkout@v4
    - run: docker info
    - run: docker buildx version
    - run: docker buildx build --help
    - run: podman version || true
    - run: rustc -Vv

    - name: Envs
      run: /home/runner/.cargo/bin/cargo-green green supergreen env
    - name: Envs again
      run: /home/runner/.cargo/bin/cargo-green green supergreen env

    - run: sudo du -sh /var/lib/docker
    - run: docker system df
    - run: docker system df --verbose
    - run: |
        docker buildx du | head || true
        docker buildx du | tail || true
    - run: docker buildx du --verbose
    - name: cargo install net=ON cache=OFF remote=OFF jobs=1
      run: |
        CARGOGREEN_LOG=trace \
        RUSTCBUILDX_LOG_PATH="$PWD"/logs.txt \
          CARGO_TARGET_DIR=~/instst cargo green -vv install --jobs=1 --locked --force hickory-dns@0.25.0-alpha.1 --features=dns-over-rustls |& tee _
    - if: ${{ failure() || success() }}
      name: 🔴 =means=> it's again that cargo issue https://github.com/rust-lang/cargo/pull/14322
      run: |
        ! grep -C20 -F "thread 'main' panicked at src/cargo/util/dependency_queue.rs:" _

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> it's again that docker issue https://github.com/moby/buildkit/issues/5217
      run: |
        ! grep -C20 -F 'ResourceExhausted: grpc: received message larger than max' logs.txt

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> there's some panic!s
      run: |
        ! grep -C20 -F ' panicked at ' logs.txt

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> there's some BUGs
      run: |
        ! grep -C20 -F 'BUG: ' logs.txt

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> here's relevant logs
      run: |
        ! grep -C20 -F ' >>> ' logs.txt

    - if: ${{ failure() || success() }}
      run: tail -n9999999 logs.txt ; echo >logs.txt
    - run: sudo du -sh /var/lib/docker
    - run: docker system df
    - run: docker system df --verbose
    - run: |
        docker buildx du | head || true
        docker buildx du | tail || true
    - run: docker buildx du --verbose

    - name: Target dir disk usage
      if: ${{ failure() || success() }}
      run: du -sh ~/instst

    - name: Ensure running the same command twice without modifications...
      run: |
        CARGOGREEN_LOG=trace \
        RUSTCBUILDX_LOG_PATH="$PWD"/logs.txt \
          CARGO_TARGET_DIR=~/instst cargo green -vv install --jobs=1 --locked --force hickory-dns@0.25.0-alpha.1 --features=dns-over-rustls |& tee _
    - name: ...doesn't recompile anything
      run: |
        err=0
        grep Finished  _ | grep -E [01]...s || err=1
        grep Dirty     _                    && err=1
        grep Compiling _                    && err=1
        exit $err
    - if: ${{ failure() || success() }}
      name: 🔴 =means=> it's again that cargo issue https://github.com/rust-lang/cargo/pull/14322
      run: |
        ! grep -C20 -F "thread 'main' panicked at src/cargo/util/dependency_queue.rs:" _

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> it's again that docker issue https://github.com/moby/buildkit/issues/5217
      run: |
        ! grep -C20 -F 'ResourceExhausted: grpc: received message larger than max' logs.txt

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> there's some panic!s
      run: |
        ! grep -C20 -F ' panicked at ' logs.txt

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> there's some BUGs
      run: |
        ! grep -C20 -F 'BUG: ' logs.txt

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> here's relevant logs
      run: |
        ! grep -C20 -F ' >>> ' logs.txt

    - if: ${{ failure() || success() }}
      run: tail -n9999999 logs.txt ; echo >logs.txt
    - run: sudo du -sh /var/lib/docker
    - run: docker system df
    - run: docker system df --verbose
    - run: |
        docker buildx du | head || true
        docker buildx du | tail || true
    - run: docker buildx du --verbose

    - name: Target dir disk usage
      if: ${{ failure() || success() }}
      run: du -sh ~/instst

  ntpd_1-2-3:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash -euo pipefail {0}
    needs: bin
    steps:
    - uses: actions-rs/toolchain@v1
      with:
        profile: minimal
        toolchain: stable


    - name: Retrieve saved bin
      uses: actions/download-artifact@v4
      with:
        name: bin-artifacts

    - name: Install saved bin
      shell: bash -eu {0}
      run: | # TODO: whence https://github.com/actions/download-artifact/issues/236
        chmod +x ./cargo-green
        { ! ./cargo-green --version ; } | grep cargo-green
        mv ./cargo-green ~/.cargo/bin/
    - uses: actions/checkout@v4
    - run: docker info
    - run: docker buildx version
    - run: docker buildx build --help
    - run: podman version || true
    - run: rustc -Vv

    - name: Envs
      run: /home/runner/.cargo/bin/cargo-green green supergreen env
    - name: Envs again
      run: /home/runner/.cargo/bin/cargo-green green supergreen env

    - run: sudo du -sh /var/lib/docker
    - run: docker system df
    - run: docker system df --verbose
    - run: |
        docker buildx du | head || true
        docker buildx du | tail || true
    - run: docker buildx du --verbose
    - name: cargo install net=ON cache=OFF remote=OFF jobs=1
      run: |
        CARGOGREEN_LOG=trace \
        RUSTCBUILDX_LOG_PATH="$PWD"/logs.txt \
          CARGO_TARGET_DIR=~/instst cargo green -vv install --jobs=1 --locked --force ntpd@1.2.3  |& tee _
    - if: ${{ failure() || success() }}
      name: 🔴 =means=> it's again that cargo issue https://github.com/rust-lang/cargo/pull/14322
      run: |
        ! grep -C20 -F "thread 'main' panicked at src/cargo/util/dependency_queue.rs:" _

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> it's again that docker issue https://github.com/moby/buildkit/issues/5217
      run: |
        ! grep -C20 -F 'ResourceExhausted: grpc: received message larger than max' logs.txt

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> there's some panic!s
      run: |
        ! grep -C20 -F ' panicked at ' logs.txt

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> there's some BUGs
      run: |
        ! grep -C20 -F 'BUG: ' logs.txt

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> here's relevant logs
      run: |
        ! grep -C20 -F ' >>> ' logs.txt

    - if: ${{ failure() || success() }}
      run: tail -n9999999 logs.txt ; echo >logs.txt
    - run: sudo du -sh /var/lib/docker
    - run: docker system df
    - run: docker system df --verbose
    - run: |
        docker buildx du | head || true
        docker buildx du | tail || true
    - run: docker buildx du --verbose

    - name: Target dir disk usage
      if: ${{ failure() || success() }}
      run: du -sh ~/instst

    - name: Ensure running the same command twice without modifications...
      run: |
        CARGOGREEN_LOG=trace \
        RUSTCBUILDX_LOG_PATH="$PWD"/logs.txt \
          CARGO_TARGET_DIR=~/instst cargo green -vv install --jobs=1 --locked --force ntpd@1.2.3  |& tee _
    - name: ...doesn't recompile anything
      run: |
        err=0
        grep Finished  _ | grep -E [01]...s || err=1
        grep Dirty     _                    && err=1
        grep Compiling _                    && err=1
        exit $err
    - if: ${{ failure() || success() }}
      name: 🔴 =means=> it's again that cargo issue https://github.com/rust-lang/cargo/pull/14322
      run: |
        ! grep -C20 -F "thread 'main' panicked at src/cargo/util/dependency_queue.rs:" _

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> it's again that docker issue https://github.com/moby/buildkit/issues/5217
      run: |
        ! grep -C20 -F 'ResourceExhausted: grpc: received message larger than max' logs.txt

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> there's some panic!s
      run: |
        ! grep -C20 -F ' panicked at ' logs.txt

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> there's some BUGs
      run: |
        ! grep -C20 -F 'BUG: ' logs.txt

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> here's relevant logs
      run: |
        ! grep -C20 -F ' >>> ' logs.txt

    - if: ${{ failure() || success() }}
      run: tail -n9999999 logs.txt ; echo >logs.txt
    - run: sudo du -sh /var/lib/docker
    - run: docker system df
    - run: docker system df --verbose
    - run: |
        docker buildx du | head || true
        docker buildx du | tail || true
    - run: docker buildx du --verbose

    - name: Target dir disk usage
      if: ${{ failure() || success() }}
      run: du -sh ~/instst

  qcow2-rs_0-1-2:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash -euo pipefail {0}
    needs: bin
    steps:
    - uses: actions-rs/toolchain@v1
      with:
        profile: minimal
        toolchain: stable


    - name: Retrieve saved bin
      uses: actions/download-artifact@v4
      with:
        name: bin-artifacts

    - name: Install saved bin
      shell: bash -eu {0}
      run: | # TODO: whence https://github.com/actions/download-artifact/issues/236
        chmod +x ./cargo-green
        { ! ./cargo-green --version ; } | grep cargo-green
        mv ./cargo-green ~/.cargo/bin/
    - uses: actions/checkout@v4
    - run: docker info
    - run: docker buildx version
    - run: docker buildx build --help
    - run: podman version || true
    - run: rustc -Vv

    - name: Envs
      run: /home/runner/.cargo/bin/cargo-green green supergreen env
    - name: Envs again
      run: /home/runner/.cargo/bin/cargo-green green supergreen env

    - run: sudo du -sh /var/lib/docker
    - run: docker system df
    - run: docker system df --verbose
    - run: |
        docker buildx du | head || true
        docker buildx du | tail || true
    - run: docker buildx du --verbose
    - name: cargo install net=ON cache=OFF remote=OFF jobs=1
      run: |
        CARGOGREEN_LOG=trace \
        RUSTCBUILDX_LOG_PATH="$PWD"/logs.txt \
          CARGO_TARGET_DIR=~/instst cargo green -vv install --jobs=1 --locked --force qcow2-rs@0.1.2  |& tee _
    - if: ${{ failure() || success() }}
      name: 🔴 =means=> it's again that cargo issue https://github.com/rust-lang/cargo/pull/14322
      run: |
        ! grep -C20 -F "thread 'main' panicked at src/cargo/util/dependency_queue.rs:" _

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> it's again that docker issue https://github.com/moby/buildkit/issues/5217
      run: |
        ! grep -C20 -F 'ResourceExhausted: grpc: received message larger than max' logs.txt

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> there's some panic!s
      run: |
        ! grep -C20 -F ' panicked at ' logs.txt

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> there's some BUGs
      run: |
        ! grep -C20 -F 'BUG: ' logs.txt

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> here's relevant logs
      run: |
        ! grep -C20 -F ' >>> ' logs.txt

    - if: ${{ failure() || success() }}
      run: tail -n9999999 logs.txt ; echo >logs.txt
    - run: sudo du -sh /var/lib/docker
    - run: docker system df
    - run: docker system df --verbose
    - run: |
        docker buildx du | head || true
        docker buildx du | tail || true
    - run: docker buildx du --verbose

    - name: Target dir disk usage
      if: ${{ failure() || success() }}
      run: du -sh ~/instst

    - name: Ensure running the same command twice without modifications...
      run: |
        CARGOGREEN_LOG=trace \
        RUSTCBUILDX_LOG_PATH="$PWD"/logs.txt \
          CARGO_TARGET_DIR=~/instst cargo green -vv install --jobs=1 --locked --force qcow2-rs@0.1.2  |& tee _
    - name: ...doesn't recompile anything
      run: |
        err=0
        grep Finished  _ | grep -E [01]...s || err=1
        grep Dirty     _                    && err=1
        grep Compiling _                    && err=1
        exit $err
    - if: ${{ failure() || success() }}
      name: 🔴 =means=> it's again that cargo issue https://github.com/rust-lang/cargo/pull/14322
      run: |
        ! grep -C20 -F "thread 'main' panicked at src/cargo/util/dependency_queue.rs:" _

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> it's again that docker issue https://github.com/moby/buildkit/issues/5217
      run: |
        ! grep -C20 -F 'ResourceExhausted: grpc: received message larger than max' logs.txt

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> there's some panic!s
      run: |
        ! grep -C20 -F ' panicked at ' logs.txt

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> there's some BUGs
      run: |
        ! grep -C20 -F 'BUG: ' logs.txt

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> here's relevant logs
      run: |
        ! grep -C20 -F ' >>> ' logs.txt

    - if: ${{ failure() || success() }}
      run: tail -n9999999 logs.txt ; echo >logs.txt
    - run: sudo du -sh /var/lib/docker
    - run: docker system df
    - run: docker system df --verbose
    - run: |
        docker buildx du | head || true
        docker buildx du | tail || true
    - run: docker buildx du --verbose

    - name: Target dir disk usage
      if: ${{ failure() || success() }}
      run: du -sh ~/instst

  ripgrep_14-1-0:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash -euo pipefail {0}
    needs: bin
    steps:
    - uses: actions-rs/toolchain@v1
      with:
        profile: minimal
        toolchain: stable


    - name: Retrieve saved bin
      uses: actions/download-artifact@v4
      with:
        name: bin-artifacts

    - name: Install saved bin
      shell: bash -eu {0}
      run: | # TODO: whence https://github.com/actions/download-artifact/issues/236
        chmod +x ./cargo-green
        { ! ./cargo-green --version ; } | grep cargo-green
        mv ./cargo-green ~/.cargo/bin/
    - uses: actions/checkout@v4
    - run: docker info
    - run: docker buildx version
    - run: docker buildx build --help
    - run: podman version || true
    - run: rustc -Vv

    - name: Envs
      run: /home/runner/.cargo/bin/cargo-green green supergreen env
    - name: Envs again
      run: /home/runner/.cargo/bin/cargo-green green supergreen env

    - run: sudo du -sh /var/lib/docker
    - run: docker system df
    - run: docker system df --verbose
    - run: |
        docker buildx du | head || true
        docker buildx du | tail || true
    - run: docker buildx du --verbose
    - name: cargo install net=ON cache=OFF remote=OFF jobs=1
      run: |
        CARGOGREEN_LOG=trace \
        RUSTCBUILDX_LOG_PATH="$PWD"/logs.txt \
          CARGO_TARGET_DIR=~/instst cargo green -vv install --jobs=1 --locked --force ripgrep@14.1.0  |& tee _
    - if: ${{ failure() || success() }}
      name: 🔴 =means=> it's again that cargo issue https://github.com/rust-lang/cargo/pull/14322
      run: |
        ! grep -C20 -F "thread 'main' panicked at src/cargo/util/dependency_queue.rs:" _

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> it's again that docker issue https://github.com/moby/buildkit/issues/5217
      run: |
        ! grep -C20 -F 'ResourceExhausted: grpc: received message larger than max' logs.txt

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> there's some panic!s
      run: |
        ! grep -C20 -F ' panicked at ' logs.txt

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> there's some BUGs
      run: |
        ! grep -C20 -F 'BUG: ' logs.txt

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> here's relevant logs
      run: |
        ! grep -C20 -F ' >>> ' logs.txt

    - if: ${{ failure() || success() }}
      run: tail -n9999999 logs.txt ; echo >logs.txt
    - run: sudo du -sh /var/lib/docker
    - run: docker system df
    - run: docker system df --verbose
    - run: |
        docker buildx du | head || true
        docker buildx du | tail || true
    - run: docker buildx du --verbose

    - name: Target dir disk usage
      if: ${{ failure() || success() }}
      run: du -sh ~/instst

    - name: Ensure running the same command twice without modifications...
      run: |
        CARGOGREEN_LOG=trace \
        RUSTCBUILDX_LOG_PATH="$PWD"/logs.txt \
          CARGO_TARGET_DIR=~/instst cargo green -vv install --jobs=1 --locked --force ripgrep@14.1.0  |& tee _
    - name: ...doesn't recompile anything
      run: |
        err=0
        grep Finished  _ | grep -E [01]...s || err=1
        grep Dirty     _                    && err=1
        grep Compiling _                    && err=1
        exit $err
    - if: ${{ failure() || success() }}
      name: 🔴 =means=> it's again that cargo issue https://github.com/rust-lang/cargo/pull/14322
      run: |
        ! grep -C20 -F "thread 'main' panicked at src/cargo/util/dependency_queue.rs:" _

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> it's again that docker issue https://github.com/moby/buildkit/issues/5217
      run: |
        ! grep -C20 -F 'ResourceExhausted: grpc: received message larger than max' logs.txt

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> there's some panic!s
      run: |
        ! grep -C20 -F ' panicked at ' logs.txt

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> there's some BUGs
      run: |
        ! grep -C20 -F 'BUG: ' logs.txt

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> here's relevant logs
      run: |
        ! grep -C20 -F ' >>> ' logs.txt

    - if: ${{ failure() || success() }}
      run: tail -n9999999 logs.txt ; echo >logs.txt
    - run: sudo du -sh /var/lib/docker
    - run: docker system df
    - run: docker system df --verbose
    - run: |
        docker buildx du | head || true
        docker buildx du | tail || true
    - run: docker buildx du --verbose

    - name: Target dir disk usage
      if: ${{ failure() || success() }}
      run: du -sh ~/instst

  rublk_0-2-0:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash -euo pipefail {0}
    needs: bin
    steps:
    - uses: actions-rs/toolchain@v1
      with:
        profile: minimal
        toolchain: stable


    - name: Retrieve saved bin
      uses: actions/download-artifact@v4
      with:
        name: bin-artifacts

    - name: Install saved bin
      shell: bash -eu {0}
      run: | # TODO: whence https://github.com/actions/download-artifact/issues/236
        chmod +x ./cargo-green
        { ! ./cargo-green --version ; } | grep cargo-green
        mv ./cargo-green ~/.cargo/bin/
    - uses: actions/checkout@v4
    - run: docker info
    - run: docker buildx version
    - run: docker buildx build --help
    - run: podman version || true
    - run: rustc -Vv

    - name: Envs
      run: /home/runner/.cargo/bin/cargo-green green supergreen env
    - name: Envs again
      run: /home/runner/.cargo/bin/cargo-green green supergreen env

    - run: sudo du -sh /var/lib/docker
    - run: docker system df
    - run: docker system df --verbose
    - run: |
        docker buildx du | head || true
        docker buildx du | tail || true
    - run: docker buildx du --verbose
    - name: cargo install net=ON cache=OFF remote=OFF jobs=1
      run: |
        CARGOGREEN_LOG=trace \
        RUSTCBUILDX_LOG_PATH="$PWD"/logs.txt \
          CARGO_TARGET_DIR=~/instst cargo green -vv install --jobs=1 --locked --force rublk@0.2.0  |& tee _
    - if: ${{ failure() || success() }}
      name: 🔴 =means=> it's again that cargo issue https://github.com/rust-lang/cargo/pull/14322
      run: |
        ! grep -C20 -F "thread 'main' panicked at src/cargo/util/dependency_queue.rs:" _

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> it's again that docker issue https://github.com/moby/buildkit/issues/5217
      run: |
        ! grep -C20 -F 'ResourceExhausted: grpc: received message larger than max' logs.txt

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> there's some panic!s
      run: |
        ! grep -C20 -F ' panicked at ' logs.txt

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> there's some BUGs
      run: |
        ! grep -C20 -F 'BUG: ' logs.txt

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> here's relevant logs
      run: |
        ! grep -C20 -F ' >>> ' logs.txt

    - if: ${{ failure() || success() }}
      run: tail -n9999999 logs.txt ; echo >logs.txt
    - run: sudo du -sh /var/lib/docker
    - run: docker system df
    - run: docker system df --verbose
    - run: |
        docker buildx du | head || true
        docker buildx du | tail || true
    - run: docker buildx du --verbose

    - name: Target dir disk usage
      if: ${{ failure() || success() }}
      run: du -sh ~/instst

    - name: Ensure running the same command twice without modifications...
      run: |
        CARGOGREEN_LOG=trace \
        RUSTCBUILDX_LOG_PATH="$PWD"/logs.txt \
          CARGO_TARGET_DIR=~/instst cargo green -vv install --jobs=1 --locked --force rublk@0.2.0  |& tee _
    - name: ...doesn't recompile anything
      run: |
        err=0
        grep Finished  _ | grep -E [01]...s || err=1
        grep Dirty     _                    && err=1
        grep Compiling _                    && err=1
        exit $err
    - if: ${{ failure() || success() }}
      name: 🔴 =means=> it's again that cargo issue https://github.com/rust-lang/cargo/pull/14322
      run: |
        ! grep -C20 -F "thread 'main' panicked at src/cargo/util/dependency_queue.rs:" _

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> it's again that docker issue https://github.com/moby/buildkit/issues/5217
      run: |
        ! grep -C20 -F 'ResourceExhausted: grpc: received message larger than max' logs.txt

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> there's some panic!s
      run: |
        ! grep -C20 -F ' panicked at ' logs.txt

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> there's some BUGs
      run: |
        ! grep -C20 -F 'BUG: ' logs.txt

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> here's relevant logs
      run: |
        ! grep -C20 -F ' >>> ' logs.txt

    - if: ${{ failure() || success() }}
      run: tail -n9999999 logs.txt ; echo >logs.txt
    - run: sudo du -sh /var/lib/docker
    - run: docker system df
    - run: docker system df --verbose
    - run: |
        docker buildx du | head || true
        docker buildx du | tail || true
    - run: docker buildx du --verbose

    - name: Target dir disk usage
      if: ${{ failure() || success() }}
      run: du -sh ~/instst

  vixargs_0-1-0:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash -euo pipefail {0}
    needs: bin
    steps:
    - uses: actions-rs/toolchain@v1
      with:
        profile: minimal
        toolchain: stable


    - name: Retrieve saved bin
      uses: actions/download-artifact@v4
      with:
        name: bin-artifacts

    - name: Install saved bin
      shell: bash -eu {0}
      run: | # TODO: whence https://github.com/actions/download-artifact/issues/236
        chmod +x ./cargo-green
        { ! ./cargo-green --version ; } | grep cargo-green
        mv ./cargo-green ~/.cargo/bin/
    - uses: actions/checkout@v4
    - run: docker info
    - run: docker buildx version
    - run: docker buildx build --help
    - run: podman version || true
    - run: rustc -Vv

    - name: Envs
      run: /home/runner/.cargo/bin/cargo-green green supergreen env
    - name: Envs again
      run: /home/runner/.cargo/bin/cargo-green green supergreen env

    - run: sudo du -sh /var/lib/docker
    - run: docker system df
    - run: docker system df --verbose
    - run: |
        docker buildx du | head || true
        docker buildx du | tail || true
    - run: docker buildx du --verbose
    - name: cargo install net=ON cache=OFF remote=OFF jobs=1
      run: |
        CARGOGREEN_LOG=trace \
        RUSTCBUILDX_LOG_PATH="$PWD"/logs.txt \
          CARGO_TARGET_DIR=~/instst cargo green -vv install --jobs=1 --locked --force vixargs@0.1.0  |& tee _
    - if: ${{ failure() || success() }}
      name: 🔴 =means=> it's again that cargo issue https://github.com/rust-lang/cargo/pull/14322
      run: |
        ! grep -C20 -F "thread 'main' panicked at src/cargo/util/dependency_queue.rs:" _

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> it's again that docker issue https://github.com/moby/buildkit/issues/5217
      run: |
        ! grep -C20 -F 'ResourceExhausted: grpc: received message larger than max' logs.txt

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> there's some panic!s
      run: |
        ! grep -C20 -F ' panicked at ' logs.txt

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> there's some BUGs
      run: |
        ! grep -C20 -F 'BUG: ' logs.txt

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> here's relevant logs
      run: |
        ! grep -C20 -F ' >>> ' logs.txt

    - if: ${{ failure() || success() }}
      run: tail -n9999999 logs.txt ; echo >logs.txt
    - run: sudo du -sh /var/lib/docker
    - run: docker system df
    - run: docker system df --verbose
    - run: |
        docker buildx du | head || true
        docker buildx du | tail || true
    - run: docker buildx du --verbose

    - name: Target dir disk usage
      if: ${{ failure() || success() }}
      run: du -sh ~/instst

    - name: Ensure running the same command twice without modifications...
      run: |
        CARGOGREEN_LOG=trace \
        RUSTCBUILDX_LOG_PATH="$PWD"/logs.txt \
          CARGO_TARGET_DIR=~/instst cargo green -vv install --jobs=1 --locked --force vixargs@0.1.0  |& tee _
    - name: ...doesn't recompile anything
      run: |
        err=0
        grep Finished  _ | grep -E [01]...s || err=1
        grep Dirty     _                    && err=1
        grep Compiling _                    && err=1
        exit $err
    - if: ${{ failure() || success() }}
      name: 🔴 =means=> it's again that cargo issue https://github.com/rust-lang/cargo/pull/14322
      run: |
        ! grep -C20 -F "thread 'main' panicked at src/cargo/util/dependency_queue.rs:" _

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> it's again that docker issue https://github.com/moby/buildkit/issues/5217
      run: |
        ! grep -C20 -F 'ResourceExhausted: grpc: received message larger than max' logs.txt

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> there's some panic!s
      run: |
        ! grep -C20 -F ' panicked at ' logs.txt

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> there's some BUGs
      run: |
        ! grep -C20 -F 'BUG: ' logs.txt

    - if: ${{ failure() || success() }}
      name: 🔴 =means=> here's relevant logs
      run: |
        ! grep -C20 -F ' >>> ' logs.txt

    - if: ${{ failure() || success() }}
      run: tail -n9999999 logs.txt ; echo >logs.txt
    - run: sudo du -sh /var/lib/docker
    - run: docker system df
    - run: docker system df --verbose
    - run: |
        docker buildx du | head || true
        docker buildx du | tail || true
    - run: docker buildx du --verbose

    - name: Target dir disk usage
      if: ${{ failure() || success() }}
      run: du -sh ~/instst

